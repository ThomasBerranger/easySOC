{% extends 'base.html.twig' %}

{% block body %}

    <main class="app-content">

        <div class="row">
            <div class="col-12">
                <div class="tile">
                    <h3 class="tile-title text-center">Alerts</h3>
                    {#                    <p><a class="btn btn-primary icon-btn" href="" target="_blank"><i class="fa fa-file"></i>Refresh</a></p>#}
                    {#                    <div class="overlay">#}
                    {#                        <div class="m-loader mr-4">#}
                    {#                            <svg class="m-circular" viewBox="25 25 50 50">#}
                    {#                                <circle class="path" cx="50" cy="50" r="20" fill="none" stroke-width="4" stroke-miterlimit="10"></circle>#}
                    {#                            </svg>#}
                    {#                        </div>#}
                    {#                        <h3 class="l-text">Loading</h3>#}
                    {#                    </div>#}
                    <canvas id="alerts-chart"></canvas>
                    {% include 'charts/template.html.twig' with chartData %}

                    <form class="row" method="post">
                        <span class="align-middle ml-5 mt-2 font-weight-bold">Compare :</span>
                        <div class="form-group col-1">
                            <select name="since" id="since" class="form-control">
                                <option value="2019">2019</option>
                                <option value="2018">2018</option>
                                <option value="2017">2017</option>
                                <option value="2016">2016</option>
                            </select>
                        </div>
                        <span class="align-middle ml-3 mt-2 font-weight-bold">With :</span>
                        <div class="form-group col-1">
                            <select name="to" id="to" class="form-control">
                                <option value="2019">2019</option>
                                <option value="2018">2018</option>
                                <option value="2017">2017</option>
                                <option value="2016">2016</option>
                            </select>
                        </div>
                        <div class="form-group col-1 align-self-end">
                            <button class="btn btn-secondary" type="submit">Apply comparison</button>
                        </div>
                    </form>

                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <div class="tile">
                    <h3 class="tile-title text-center">Alerts history</h3>
                    <table id="alerts" class="table table-hover table-bordered dataTable no-footer" width="100%">
                        <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Type</th>
                            <th scope="col">Date</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for alert in alerts %}
                            <tr>
                                <th scope="row">{{ loop.index }}</th>
                                <td>{{ alert.type }}</td>
                                <td>{{ alert.createdAt|date('d/m/Y') }}</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    </main>

{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready( function () {
            $('#alerts').DataTable();
        } );
    </script>
{% endblock %}
