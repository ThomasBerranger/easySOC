{% extends 'base.html.twig' %}

{% block title 'Company Edit' %}

{% block body %}

    <main class="app-content">
        <div class="container">

            <div class="row">
                <div class="col-12">
                    <div class="tile">
                        <h3 class="tile-title text-center">Company edition</h3>

                        {{ form_start(form) }}

                        {{ form_row(form.name) }}

                        <div class="text-center">
                            <button type="submit" class="btn btn-outline-success"><i class="fas fa-save"></i> Save changes</button>
                        </div>

                        {{ form_end(form) }}

                    </div>
                </div>
            </div>

        </div>
    </main>

{% endblock %}
